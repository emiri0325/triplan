<h1 class="users-heading">ユーザー一覧</h1>

  <ul class="list-unstyled">
    <% @users.each do |user| %>

        <div>
          <%= user.name %>
        </div>
        <div>
          <p><%= link_to 'View profile', user_path(user) %></p>
        </div>

    <% end %>
  </ul>  
